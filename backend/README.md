# Campus Travelling System backend

Spring Boot module. Prefer opening the **repository root** in IntelliJ, not this folder alone.

From IntelliJ: run configuration **Campus Travelling System**.

From a terminal:

```powershell
cd backend
mvn spring-boot:run
```

App URL: http://localhost:8080/login.html
